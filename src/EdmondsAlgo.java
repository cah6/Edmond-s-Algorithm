/**
 * The actual algorithm, encapsulated into it's own class.
 * 
 * @author christianhenry
 *
 */
public class EdmondsAlgo {
	
	/**
	 * Only function called from outside -- computes the minimum conversation tree from the given
	 * root node and adjacency list.
	 * @param root - root node to start conversation at
	 * @param adjacencies - already made AdjacencyList representing the input graph. Must contain root node.
	 * @return AdjacencyList with only the edges of the minimum conversation tree.
	 */
	public AdjacencyList getMinTree(Node root, AdjacencyList adjacencies) {
		return null;
	}
}
